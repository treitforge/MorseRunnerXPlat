[project]
name = "morserunnerxplat-tooling"
version = "0.0.0"
description = "Repository-local Python tooling for MorseRunnerXPlat"
requires-python = ">=3.13,<3.14"
dependencies = [
    "pyyaml>=6.0.2,<7",
]

[tool.uv]
package = false
